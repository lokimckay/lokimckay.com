---
import type { HTMLAttributes } from "astro/types";
import { storageHost } from "@/lib/config";

interface Props extends HTMLAttributes<"img"> {}
const { src } = Astro.props;
const url = `${storageHost}${src}`;
---

<img src={url} {...Astro.props} />
